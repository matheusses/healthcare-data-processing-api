# Replace OPENAI_API_KEY and LANGSMITH_API_KEY before use, or patch after apply:
#   kubectl edit secret api-secret -n healthcare-api
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://user:change-me@postgres.healthcare-api.svc.cluster.local:5432/healthcare"
  DOCUMENT_STORAGE_ACCESS_KEY: "minioadmin"
  DOCUMENT_STORAGE_SECRET_KEY: "minioadmin"
  OPENAI_API_KEY: ""
  LANGSMITH_API_KEY: ""
