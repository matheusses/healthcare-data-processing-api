# Copy this file to .env and set values. Do not commit .env or any secrets.
# Required for API: DATABASE_URL, document storage (MinIO). Optional: OTEL_*, OPENAI_*, LANGSMITH_*.

# Database (required)
POSTGRES_USER=user_dev
POSTGRES_PASSWORD=change_me
POSTGRES_DB=healthcare
# For local/host: use localhost and port 5434 if Postgres is from docker-compose.
# Docker Compose sets DATABASE_URL for the api service automatically.
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5434/${POSTGRES_DB}
PG_TRGM_SIMILARITY_THRESHOLD=0.2

# OpenTelemetry (optional; leave empty to disable OTLP export)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=healthcare-api
OTEL_TRACES_SAMPLER=parentbased_traceidratio
OTEL_TRACES_SAMPLER_ARG=1.0

# LangSmith (optional; LLM tracing and OTEL export to LangSmith when LANGSMITH_API_KEY is set)
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING=true
# LANGSMITH_OTEL_EXPORT=true
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Document storage (required for note uploads; MinIO / S3-compatible)
DOCUMENT_STORAGE_ENDPOINT=http://localhost:9000
DOCUMENT_STORAGE_BUCKET=patient-notes
DOCUMENT_STORAGE_ACCESS_KEY=minioadmin
DOCUMENT_STORAGE_SECRET_KEY=minioadmin
DOCUMENT_STORAGE_REGION=us-east-1
# Optional MinIO root credentials used by docker-compose provisioning
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Vector / embeddings (optional; leave empty to skip embedding pipeline)
VECTOR_EMBEDDING_MODEL=text-embedding-3-small
VECTOR_EMBEDDING_DIMENSIONS=1536
CHUNK_SIZE=800
CHUNK_OVERLAP=100
OPENAI_API_KEY=
OPENAI_TEMPERATURE=0
# Nucleus sampling (0–1). Must be <= 1 per OpenAI API.
OPENAI_TOP_P=1

# Summary and chat (required for GET /patients/{id}/summary and POST /patients/{id}/chat)
OPENAI_SUMMARY_MODEL=gpt-4o-mini
OPENAI_CHAT_MODEL=gpt-4o-mini

# Environment
ENVIRONMENT=development
